# V2.7.1 — أماكن تخزين مخصصة

- نقل قاعدة SQLite الحالية إلى مجلد يختاره المستخدم، مثل D:\Financial Manager Data.
- فحص سلامة النسخة الجديدة قبل اعتمادها.
- عدم حذف قاعدة البيانات السابقة تلقائياً بعد النقل.
- اختيار قاعدة بيانات موجودة، مهم بعد فورمات C أو إعادة تثبيت Windows.
- اختيار مجلد مستقل للنسخ الاحتياطية.
- حفظ مسار النسخ الاحتياطي داخل قاعدة البيانات أيضاً ليستعاد عند ربط القاعدة لاحقاً.
- عدم استبدال قاعدة مخصصة بقاعدة فارغة تلقائياً عند تعذر الوصول إليها.
