# V2.7.0 — SQLite Database Edition

## ما تغيّر
- أصبح SQLite هو التخزين الأساسي بدل ملف JSON.
- لا يحتاج المستخدم إلى تثبيت SQLite أو SQL Server أو أي برنامج قاعدة بيانات خارجي.
- قاعدة البيانات تُنشأ تلقائياً داخل مجلد بيانات التطبيق باسم `finance.db`.
- فصل البيانات في جداول: الوارد، المنصرف، المبالغ المطلوبة، دفعاتها، البنوك، الأرصدة، القروض، الأقساط، ودفعات القروض.
- جميع عمليات حفظ الحالة تتم داخل Transaction واحدة مع `WAL` و `synchronous=FULL`.

## ترقية البيانات القديمة
- إذا وجد البرنامج `financial-data.json` القديم وقاعدة البيانات جديدة، يقوم بالتحويل تلقائياً.
- قبل التحويل يتم نسخ ملف JSON القديم إلى مجلد `backups/migration`.
- بعد التحويل تتم مقارنة أعداد السجلات والإجماليات المالية الأساسية.
- إذا فشل التحقق لا يعتمد البرنامج قاعدة البيانات الجديدة ولا يحذف الملف القديم.
- بعد نجاح التحويل يتم إنشاء نسخة SQLite بعد الترحيل أيضاً.

## النسخ الاحتياطية
- نسخة يومية محلية تلقائية داخل `backups/daily` مع الاحتفاظ بآخر 30 يوم.
- أثناء العمل يتم تحديث النسخة اليومية دورياً بحد أقصى مرة كل 10 دقائق عند وجود تغييرات.
- زر `نسخة احتياطية الآن` موجود في الإعدادات ويُنشئ ملف DB مستقل.
- استيراد/تصدير JSON ما زال موجوداً للتوافق والنقل اليدوي.

## التشغيل
```powershell
npm install
npm run dev
```
لا تنشئ قاعدة البيانات يدوياً؛ التطبيق ينشئها تلقائياً.
