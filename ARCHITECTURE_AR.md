# هيكلة المشروع — V2.6.0

تم تنفيذ Refactor بدون تغيير صيغة البيانات أو واجهة البرنامج.

## نقطة التشغيل
- `src/main.jsx`: تشغيل React واستيراد الخط والتنسيقات فقط.
- `src/App.jsx`: إدارة الحالة العامة والتنقل بين الصفحات وربط البيانات.

## التخزين والبيانات
- `src/store/state.js`: الحالة الابتدائية، ترقية/تطبيع البيانات القديمة، وقراءة التخزين.
- `src/utils/appUtils.js`: التواريخ، تنسيق الأرقام، المبالغ، المعرفات ورسائل تأكيد الحذف.

## المكونات المشتركة
- `src/components/common.jsx`: Sidebar، Header، Date Picker، حقول الإدخال المشتركة، Error Boundary وترويسة تقارير الطباعة.

## الصفحات والميزات
- `src/features/reports/ReportsPage.jsx`: الوارد والمنصرف.
- `src/features/pending/PendingPage.jsx`: المبالغ المطلوبة، الاستيراد والسداد الجزئي.
- `src/features/banks/BankBalancesPage.jsx`: أرصدة البنوك وطباعة الأرصدة.
- `src/features/loans/LoansPage.jsx`: تسجيل القروض، الأقساط، بيان القروض، Excel والطباعة.
- `src/features/settings/SettingsPage.jsx`: الأقسام والإعدادات.

## الطباعة
- `src/print/preview.js`: إنشاء HTML المعاينة فقط.
- قوالب كل تقرير موجودة داخل Feature الخاصة به، فلا يعتمد تقرير البنوك على صفحة القروض أو العكس.

## التنسيق
- `src/styles/app.css`: التنسيقات الحالية كاملة في ملف مستقل عن JavaScript.

الهدف من الهيكلة هو تقليل الاعتماد المتبادل وتسهيل أي تعديل لاحق بدون لمس ملف ضخم واحد.
